import { useEffect, useState } from "react";
const items=[{question:"Which delivery option should I choose?",answer:"Choose Vilnius delivery for the fixed hub option, or door-to-door when you want the local leg included to the final handover address."},{question:"Is the displayed route final?",answer:"The route and estimate are used for booking. Operational timing can still be refined when the driver is assigned and pickup is confirmed."},{question:"What happens after I choose?",answer:"Your selected route and price are carried into the next step, where you sign in and continue the booking."}];
export function DeliveryMarketFaqFixture(){const[expanded,setExpanded]=useState(false);const[active,setActive]=useState(0);useEffect(()=>{if(expanded||window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;const t=window.setInterval(()=>setActive(v=>(v+1)%items.length),4000);return()=>window.clearInterval(t)},[expanded]);return <section className="delivery-faq-stage" aria-label="Delivery Market FAQ capture stress fixture"><div className="delivery-faq-copy"><p>REAL APPLICATION DOM</p><h2>Delivery Market FAQ strip</h2><span>Scroll this stateful component beneath the fixed glass.</span></div><div data-journey-faq className="delivery-faq-strip"><button type="button" onClick={()=>setExpanded(v=>!v)} aria-expanded={expanded} className="delivery-faq-trigger"><span className="delivery-faq-label">FAQ</span><span className="delivery-faq-question">{items[active]?.question}</span><span className="delivery-faq-count">{active+1}/{items.length}</span><span className="delivery-faq-chevron" aria-hidden="true">{expanded?"⌃":"⌄"}</span></button>{expanded?<div className="delivery-faq-expanded">{items.map(item=><div key={item.question} className="delivery-faq-item"><p>{item.question}</p><span>{item.answer}</span></div>)}</div>:null}</div></section>}
